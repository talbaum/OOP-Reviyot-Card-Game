#include "Card.h"
#include "Hand.h"
#include "Deck.h"
#include "Player.h"
#include <iostream>
#include <vector>
#include <string>
using namespace std;

/*int main (int argc, char *argv[]){


NumericCard * b= new NumericCard('S',4);
	b->toString();

	/*
	NumericCard *a=new NumericCard("H",2);

	NumericCard d("S",2);
    NumericCard g("D",2);
    NumericCard y("H",2);
    cout << "hello" << endl;
    cout << d.toString() << endl;
    FigureCard *b = new FigureCard("D","J");
     FigureCard *e= new FigureCard("S","J");
     FigureCard c("C","A");
    cout << c.toString() << endl;
    //cout << a.toString() << endl;
    Hand h(12);
    vector <int> arr(2);
    cout <<arr.at(1) << endl;
    cout << h.toString() << endl;
   /* Card* lulu=new NumericCard(a);
    Hand *h=new Hand(lulu);
    h.addCard(y);
    h.addCard(d);
    h.addCard(c);
    //h.addCard(b);
   // h.addCard(a);
    h.addCard(g);
    cout << h.toString() << endl;
    h.removeValue(h.search(2,0));
    cout << h.toString() << endl;
    vector <Card*> deck(0);
    deck.push_back(a);
        deck.push_back(b);
            deck.push_back(e);


    Deck* d1=new Deck(deck);
        cout <<"========="<< endl;
    cout << d1->toString() << endl;
    string s("paz");
    //PlayerType1* p1= new PlayerType1 (s,1);//doing problems, needs to be fixed!!!!!!!!!!!!!!!



   /* if(h.getHasSeries()==false)
         cout << "kaki1" << endl;
    h.addCard(y);
    cout << h.getNumberOfCards() << endl;
    cout << h.toString() << endl;
    h.removeCard(b);
    cout << h.toString() << endl;
    cout << h.getNumOfValue(2) << endl;

   Deck s;
    //Card* z = new NumericCard("C", 15);
    //cout << z->toString() << endl;
    cout <<s.toString() << endl;
    cout <<s.getNumberOfCards() << endl;
    s.fetchCard();
    cout <<s.getNumberOfCards() << endl;
    cout <<s.toString() << endl;
    if(h.getHasSeries()==true)
        cout << "kaki" << endl;*/
      //     cout << h.getMyHand().size() << endl;


//return 0;
//}
